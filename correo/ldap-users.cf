# --- correo/ldap-users.cf ---
server_host = 172.20.0.16
search_base = ou=People,dc=javier,dc=local
version = 3
bind = no
# Consulta: Buscar por uid o mail y devolver la carpeta del usuario
query_filter = (|(uid=%s)(mail=%s))
result_attribute = uid
result_format = %s/Maildir/